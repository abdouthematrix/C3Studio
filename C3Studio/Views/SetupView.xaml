<UserControl
    x:Class="C3Studio.Views.SetupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cv="clr-namespace:C3Studio.Converters"
    xmlns:vm="clr-namespace:C3Studio.ViewModels"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    Background="{StaticResource Br.BgDeep}">
    <ScrollViewer>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>



            <StackPanel Grid.Column="1">

                <!--  Header  -->
                <StackPanel>
                    <TextBlock
                        FontSize="36"
                        Style="{StaticResource TitleLabel}"
                        Text="C3Studio" />
                    <TextBlock Style="{StaticResource SubTitle}" Text="Conquer Online Asset Browser &amp; Editor" />
                    <Rectangle
                        Height="1"
                        Margin="0,10,0,0"
                        Fill="{StaticResource Br.Border}" />
                </StackPanel>

                <!--  Path picker  -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionLabel}" Text="GAME DIRECTORY" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource PathBox}"
                                Text="{Binding ConquerPath, UpdateSourceTrigger=PropertyChanged}" />
                            <Button
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Command="{Binding BrowseCommand}"
                                Content="Browse…"
                                Style="{StaticResource GhostButton}" />
                        </Grid>

                        <!--  Status  -->
                        <TextBlock
                            FontSize="12"
                            Opacity="0.85"
                            Text="{Binding StatusText}"
                            TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Br.Fail}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsValid}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource Br.Ok}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ValidationItems.Count}" Value="0">
                                            <Setter Property="Foreground" Value="{StaticResource Br.TextSecond}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!--  Validation checklist  -->
                <Border Style="{StaticResource Card}" Visibility="{Binding ValidationItems.Count, Converter={StaticResource ZeroToCollapsed}}">
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,12"
                            Style="{StaticResource SectionLabel}"
                            Text="VALIDATION CHECKLIST" />
                        <ItemsControl>
                            <ItemsControl.Resources>
                                <CollectionViewSource x:Key="GroupedItems" Source="{Binding ValidationItems}">
                                    <CollectionViewSource.GroupDescriptions>
                                        <PropertyGroupDescription PropertyName="Category" />
                                    </CollectionViewSource.GroupDescriptions>
                                </CollectionViewSource>
                            </ItemsControl.Resources>

                            <ItemsControl.ItemsSource>
                                <Binding Source="{StaticResource GroupedItems}" />
                            </ItemsControl.ItemsSource>

                            <!--  Wrap groups horizontally  -->
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.Panel>
                                        <!--  Each group stacks its items vertically  -->
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical" />
                                        </ItemsPanelTemplate>
                                    </GroupStyle.Panel>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="GroupItem">
                                            <Setter Property="Margin" Value="8" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="GroupItem">
                                                        <Border
                                                            Margin="4"
                                                            Padding="8"
                                                            BorderBrush="{StaticResource Br.Border}"
                                                            BorderThickness="1"
                                                            CornerRadius="6">
                                                            <StackPanel>
                                                                <!--  Group header  -->
                                                                <TextBlock
                                                                    Margin="0,0,0,6"
                                                                    FontWeight="Bold"
                                                                    Foreground="{StaticResource Br.TextSecond}"
                                                                    Text="{Binding Name}" />
                                                                <!--  Group items  -->
                                                                <ItemsPresenter />
                                                            </StackPanel>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </GroupStyle.ContainerStyle>
                                </GroupStyle>
                            </ItemsControl.GroupStyle>

                            <!--  Item template  -->
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border
                                        Margin="0,1"
                                        Padding="8,6"
                                        Background="{Binding Category, Converter={StaticResource CatToBrush}}"
                                        CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="22" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Grid.Column="0"
                                                VerticalAlignment="Center"
                                                FontSize="13"
                                                FontWeight="Bold"
                                                Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}"
                                                Text="{Binding Status, Converter={StaticResource StatusToGlyph}}" />
                                            <Border
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Style="{StaticResource ChipBadge}">
                                                <TextBlock
                                                    FontSize="10"
                                                    Foreground="{StaticResource Br.TextSecond}"
                                                    Text="{Binding Category}" />
                                            </Border>
                                            <TextBlock
                                                Grid.Column="2"
                                                VerticalAlignment="Center"
                                                FontFamily="Consolas"
                                                FontSize="12"
                                                Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}"
                                                Text="{Binding Name}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!--  Load button  -->
                <Grid>
                    <Button
                        Height="44"
                        HorizontalAlignment="Stretch"
                        Command="{Binding LoadCommand}"
                        Content="Load Game Data  →"
                        FontSize="14"
                        Style="{StaticResource PrimaryButton}"
                        Visibility="{Binding IsLoading, Converter={StaticResource InvBoolToVis}}" />
                    <Border
                        Height="44"
                        Background="{StaticResource Br.BgElevated}"
                        CornerRadius="6"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,12,0"
                                VerticalAlignment="Center"
                                FontSize="16"
                                Foreground="{StaticResource Br.Gold}"
                                Text="⟳" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="{StaticResource Br.TextSecond}"
                                Text="Loading game data…" />
                        </StackPanel>
                    </Border>
                </Grid>

                <!--  Footer hint  -->
                <TextBlock
                    FontSize="11"
                    Opacity="0.5"
                    Style="{StaticResource SubTitle}"
                    Text="C3Studio reads your local Conquer Online client files. No network connection required."
                    TextAlignment="Center"
                    TextWrapping="Wrap" />

            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>
